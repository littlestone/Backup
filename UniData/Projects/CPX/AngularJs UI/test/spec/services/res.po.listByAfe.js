'use strict';

describe('Service: res.po.listByAfe.js', function () {

  // load the service's module
  beforeEach(module('cpxUiApp'));

  // instantiate service
  var res.po.listByAfe.js;
  beforeEach(inject(function (_res.po.listByAfe.js_) {
    res.po.listByAfe.js = _res.po.listByAfe.js_;
  }));

  it('should do something', function () {
    expect(!!res.po.listByAfe.js).toBe(true);
  });

});
